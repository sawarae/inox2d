# Project Rules

## PR Creation

This repository is a fork of `Inliquid-Technologies/inox2d`.
When creating PRs with `gh` CLI, always target the fork's own main branch:

```
gh pr create --repo sawarae/inox2d --base main
```

Do NOT use the default behavior, which targets the upstream repository.
